// names export
const add = (a, b) => a + b;
export const multiply = (a, b) => a * b;

// export default function substract(a, b) {
//   return a - b;
// }

// default export
// const THRESHOLD = 5.6;
// export default THRESHOLD;
