interface AnswerButtonProps {
  option: string;
}

const AnswerButton = ({ option }: AnswerButtonProps) => {
  return (
    // <button
    //   onClick={() => addAnswer(option)}
    //   className="bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded-lg shadow-md transition duration-300"
    // >
    //   {option}
    // </button>
    <p className="text-gray-500">⚠️ MÉG NINCS IMPLEMENTÁLVA</p>
  );
};

export default AnswerButton;
